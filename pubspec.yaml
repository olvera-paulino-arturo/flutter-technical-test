name: contpaqi_technical_test
description: "A Flutter Movies project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.4.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  google_fonts: ^6.2.1
  bloc: ^9.0.0
  flutter_bloc: ^9.1.1
  flutter_conditional_rendering: ^2.1.0
  equatable: ^2.0.7
  collection: ^1.19.1
  shimmer: ^3.0.0
  flutter_secure_storage: ^9.2.4
  flutter_appauth: ^9.0.1
  google_maps_flutter: ^2.12.2
  local_auth: ^2.3.0
  geolocator: ^14.0.1
  permission_handler: ^12.0.0+1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true

  assets:
    - assets/images/
    - assets/